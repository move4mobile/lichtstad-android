<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Trim the padding from the launcher icon, or it would look too small -->
    <item
        android:bottom="-18dp"
        android:drawable="@drawable/ic_launcher_foreground"
        android:left="-18dp"
        android:right="-18dp"
        android:top="-18dp" />
</layer-list>